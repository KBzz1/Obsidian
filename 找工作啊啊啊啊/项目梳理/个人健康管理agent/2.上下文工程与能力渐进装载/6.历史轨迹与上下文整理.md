---
tags:
  - 项目/个人健康管理agent
  - 第二章/上下文工程
章节: 2
类型: 概念笔记
核心概念: 200k tokens 预算 / 确定性整理
状态: 已整理
---

> [!info] 概念归属
> 本笔记是「历史轨迹与上下文整理」的唯一归属地。其他笔记引用本概念时通过链接指向这里，不再复述。章节整体边界见 [[找工作啊啊啊啊/项目梳理/个人健康管理agent/2.上下文工程与能力渐进装载/1.总览#章节边界|1.总览#章节边界]]。

# 历史轨迹与上下文整理

## 默认不提前压缩

常规任务流转中，系统保留原始的自然语言对话和工具执行轨迹，不提前介入上下文压缩。当模型输入上下文规模逼近约 20 万令牌的系统预算时，运行时才主动拉起一次上下文整理流程。

## 为什么设 20 万预算

阈值设在 20 万令牌是出于工程工作区间的考虑。即使底层模型宣称支持更大的上下文窗口，系统也要为后续可能产生的大体积工具结果、密集型任务产物输出预留缓冲，避免触碰到物理上限时被迫做暴力截断。

## 整理策略

上下文整理默认用确定性的程序化规则裁剪与重组，不引入额外的 LLM 摘要调用。

清理的对象是已失效或被结构化结果替代的数据层：过期的历史状态栏、废弃的路由探针结果、重复的系统级指导信息，以及执行已完成、状态已收敛进正式任务产物的冗余工具调用过程。

对于仍有业务效用的运行状态，系统不靠旧历史记录维持：整理结束后，用最新结构化数据重新生成工作状态栏和派生视图（见 [[4.AgentStatus 当前工作状态栏]]、[[5.程序化派生上下文]]）。

整理严格保证执行中语义单元的完整性：进行中的工具调用指令、返回结果及其衍生的后续动作作为原子块保留，不强制分割。

## 示例：上下文整理只保留当前有效事实

一个长期任务运行数百轮后，上下文规模可能逼近预算：

```json
{
  "messages": 420,
  "tool_results": 86,
  "draft_versions": 12,
  "estimated_context_tokens": 203500
}
```

Harness 在调用模型前检查预算：

```python
if estimated_tokens > 200000:
    run_context_compaction()
```

整理器执行确定性裁剪：

```text
删除：
- 旧状态栏
- 已完成节点的临时说明
- 旧路由决策输出
- 已被新版本替代的草稿
- 已经转化为结构化产物的大型工具结果

保留：
- 当前草稿
- 当前任务状态
- 当前用户约束
- 当前工作流步骤
- 仍会影响后续决策的工具结果
```

例如原始训练历史查询结果有 3 万令牌：

```json
{
  "tool_name": "query_training_history",
  "token_count": 30000,
  "result": "过去 90 天的完整训练记录"
}
```

业务服务已经生成结构化结果：

```yaml
训练历史分析结果:
  average_frequency_per_week: 4
  recent_volume_trend: increasing
  chest_training_frequency: 2
  last_deload_date: 2025-02-10
```

整理后模型只保留结构化结果，原始工具结果仍保存在 Trace Store 中。上下文整理优先依赖结构化事实和程序化裁剪，而不是让另一个模型自由摘要：自由摘要可能丢失「60 分钟上限」「避免过顶推举」等明确约束。

## 整理后的新基线

```text
长任务上下文
      │
      │ >= 20 万令牌
      ▼
确定性上下文整理
      │
      ├── 当前有效任务状态
      ├── 当前任务产物
      ├── 程序化派生信息
      ├── 尚未解决的用户要求
      └── 必要近期原始消息
      │
      ▼
新的工作上下文基线
```

这个设计里有一个明确的取舍：系统主动承担整理带来的单次前缀缓存失效成本，拒绝为了维持高键值缓存命中率而无限期囤积低价值历史数据（缓存布局见 [[7.KV Cache 与上下文布局]]）。

## 边界问答

**1. Q：** 整理为什么默认不用 LLM 摘要？

**A：** 整理优先采用确定性的程序化规则裁剪与重组，默认不引入额外的 LLM 摘要调用；过期状态栏和派生视图由最新结构化数据重新生成，不需要模型来总结。

**2. Q：** 为什么进行中的工具调用必须作为原子块保留？

**A：** 进行中的工具调用指令、返回结果和衍生后续动作是尚未收敛的语义单元，整理时作为原子块保留，不予强制分割。

**3. Q：** 20 万阈值为什么不贴着模型的窗口上限设？

**A：** 要给后续的大体积工具结果和任务产物输出预留缓冲，避免碰到物理上限时被迫暴力截断。20 万是工程工作区间，不是模型能力上限。

**4. Q：** 为什么不用另一个模型做自由摘要来整理上下文？

**A：** 自由摘要是概率性产出，可能丢失「60 分钟上限」「避免过顶推举」这类明确约束。结构化事实和程序化裁剪保留全部约束与当前有效状态，只在必要时保留仍影响后续决策的原始结果。
