---
tags:
  - 项目/个人健康管理agent
  - 第五章/多智能体
章节: 5
类型: 概念笔记
核心概念: Task DAG / CrossDomainConstraint / 部分结果
状态: 已整理
---

> [!info] 概念归属
> 本笔记是「跨领域协作、冲突与失败语义」的唯一归属地：单领域与跨领域的执行路径、子任务（ChildTask）依赖（任务依赖图，Task DAG）、跨领域约束（CrossDomainConstraint）、失败与部分结果的收口规则。通信结构定义见 [[6.通信协议与 Artifact]]。

# 跨领域协作与冲突

## 单领域任务

单领域任务只有一个明确所有者：

```text
User
 ↓
Supervisor
 ↓
Workout Agent
 ↓
Workout Artifact
 ↓
Supervisor
 ↓
User
```

会话主管（Supervisor）不介入训练智能体（Workout Agent）的具体领域推理，只负责定义目标、等待结果以及将结果整合进主会话。

## 跨领域任务与依赖判断

跨领域任务由会话主管判断依赖关系。

如果两个任务互不依赖，例如「分别分析我本周训练情况和饮食情况」，可以在同一次请求内并行执行：

```text
                 ┌─ Workout Agent ── WorkoutAnalysis
Supervisor ──────┤
                 └─ Diet Agent ───── DietAnalysis
                          ↓
                    Supervisor
                          ↓
                       summary
```

如果后一个任务依赖前一个结果，例如「根据我最近的训练负荷调整饮食」，则顺序执行：

```text
Supervisor
    ↓
Workout Task
    ↓
TrainingLoadArtifact
    ↓
Diet Task
    ↓
NutritionArtifact
    ↓
Supervisor
```

会话主管在这里承担持续的编排，而不是一次性路由。

## Task DAG

跨领域依赖以任务依赖图（Task DAG，有向无环的任务依赖图）表达：

```python
class ChildTask:
    task_id: str                     # 子任务身份
    assigned_agent: str              # 被指派执行的 Agent

    status: Literal[                 # 任务状态
        "pending",                   # 待执行
        "running",                   # 执行中
        "waiting_input",             # 等待用户输入
        "succeeded",                 # 成功
        "failed",                    # 失败
        "cancelled"                  # 已取消
    ]

    depends_on: list[str]            # 前置任务，只有它们成功后才 runnable

    input_artifact_refs: list[str]   # 输入 Artifact 引用
    output_artifact_refs: list[str]  # 输出 Artifact 引用
```

只有 `depends_on` 中的必要前置任务已经成功完成，后续任务才能进入可执行（runnable）状态。

智能体不直接互相调用（训练智能体 ↔ 饮食智能体），而始终是训练智能体→ 会话主管（Supervisor）→ 饮食智能体（Diet Agent）。这样任务所有权（Task Ownership）、终止条件和跨域冲突都集中在会话主管层管理。

## 跨领域冲突处理

跨领域协作允许智能体提出影响其他领域的约束，但不能越过业务所有权直接修改其他领域状态。

例如训练智能体（Workout Agent）得出「high training load → additional carbohydrate availability required」，它可以生成：

```python
CrossDomainConstraint(
    source="workout",                 # 约束来源领域
    target="diet",                    # 约束作用的目标领域
    type="nutrition_requirement",     # 约束类型
    payload={...}                     # 约束的结构化载荷
)
```

会话主管（Supervisor）将该产物（Artifact）或约束（Constraint）作为下一次饮食任务（Diet Task）的输入。如果饮食智能体判断当前饮食限制无法满足该要求，则返回 `status = "conflict"`，而不是自行修改训练计划（Workout Plan）。会话主管再根据任务目标决定是否创建一个新的训练任务（Workout Task）：

```text
Workout
   ↓
constraint
   ↓
Diet
   ↓
conflict
   ↓
Supervisor
   ↓
Workout revision task
```

这种协作允许形成多跳，但不会让智能体无限制互相讨论。每个子任务（ChildTask）都必须具有明确的 `required_output_type` 和终止状态，会话主管（Supervisor）对委派深度和任务数量设置配置化执行预算。完整的资源预算（Budget）和背压（Backpressure）在第六章定义。

## 失败与部分结果

所有子智能体（Subagent）调用必须返回明确的终止状态（terminal status），不能只返回一段自然语言让会话主管猜测执行是否成功。

如果一个跨领域任务需要训练（Workout）与饮食（Diet）两个结果才能成立，而训练任务（Workout Task）失败，则依赖训练产物（Workout Artifact）的饮食任务（Diet Task）不会启动，会话主管（Supervisor）也不能将现有部分信息包装成「完整结果」。

如果两个任务互不依赖，可以允许 `Workout = succeeded, Diet = failed`，然后根据任务策略决定返回部分结果还是整体失败。是否允许部分结果（partial result）由父任务（父 Task）在创建子任务（ChildTask）时明确声明，而不是由会话主管临时发挥。

领域智能体（Domain Agent）内部的工具重试（retry）、超时（timeout）、幂等和补偿不在这一章处理。智能体只消费工具运行时（Runtime）返回的确定执行结果。

## 边界问答

**1. Q：** 为什么智能体不直接互相调用，而要经过会话主管（Supervisor）？

**A：** 直接互相调用会把任务所有权（Task Ownership）、终止条件和跨域冲突分散到各智能体。始终走训练智能体→ 会话主管→ 饮食智能体（Diet Agent），让这些职责集中在会话主管（Supervisor）层管理。

**2. Q：** 跨领域依赖为什么用任务依赖图（Task DAG）而不是让模型读聊天推断？

**A：** 只有 `depends_on` 中的必要前置任务成功后，后续任务才可执行（runnable）。依赖关系由结构化状态确定，父任务恢复靠确定性的任务（Task）状态，而不是让模型重新读整段聊天猜测执行到哪。

**3. Q：** 饮食智能体满足不了训练负荷约束，为什么不自己改训练计划（Workout Plan）？

**A：** 改训练计划（Workout Plan）是越过业务所有权。饮食智能体（Diet Agent）返回 `status = "conflict"`，把冲突交给会话主管决定是否创建新的训练修订任务，形成多跳但不越权。

**4. Q：** 为什么部分结果（partial result）要由父任务（父 Task）声明，而不是会话主管（Supervisor）临时决定？

**A：** 是否允许部分结果取决于任务本身的语义（两个结果是否缺一不可），属于父任务（父 Task）的创建参数。让会话主管每次临时发挥，会把策略判断散布到运行时，也无法在创建时就明确下游依赖能否启动。

**5. Q：** 一个跨领域任务缺一个结果，为什么不能把现有部分信息包装成完整结果？

**A：** 依赖产物（Artifact）的任务（Task）在依赖失败时不会启动，现有信息不足以支撑结论。把部分结果包装成完整结果会掩盖失败，会话主管（Supervisor）只能如实返回失败或按声明的部分结果（partial result）策略处理。
